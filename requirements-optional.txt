# Optional Dependencies for Enhanced Features
# Install these separately after core requirements are working

# Voice Control Dependencies
# Note: pyaudio may require system dependencies (portaudio19-dev on Ubuntu/Debian)
SpeechRecognition>=3.10.0
pyttsx3>=2.90

# Audio dependencies (install system packages first)
# For Ubuntu/Debian: sudo apt-get install portaudio19-dev python3-dev
# pyaudio>=0.2.11

# Computer Vision Dependencies  
# Note: These are large packages, install only if object following is needed
opencv-python>=4.8.0
numpy>=1.24.0,<2.0
Pillow>=10.0.0

# Communication protocols (optional)
asyncio-mqtt>=0.13.0

# Installation Instructions:
# 1. First install core requirements: pip install -r requirements.txt
# 2. Then install optional ones: pip install -r requirements-optional.txt
# 3. For audio support on Linux: sudo apt-get install portaudio19-dev python3-dev
# 4. Then: pip install pyaudio
